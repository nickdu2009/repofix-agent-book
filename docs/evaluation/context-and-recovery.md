# 上下文选择与失败恢复

完成基础评测后，再根据失败案例增强上下文能力。

推荐顺序：

1. 使用 `rg` 搜索代码。
2. 生成仓库目录摘要。
3. 识别主要语言和测试命令。
4. 只读取相关文件片段。
5. 限制单文件和总上下文大小。
6. 记录已经读取的文件。
7. 识别重复工具调用。
8. 测试失败后重新规划。

第一版不需要向量数据库。代码搜索、目录结构和测试反馈通常足以支持 MVP。

Python 在这一部分学习：

- 迭代器和生成器。
- `asyncio` 并发。
- 缓存。
- 字符串和文本处理。
- 上下文预算数据结构。
- 可取消任务。
